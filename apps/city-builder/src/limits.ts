/** Acquisition budgets are independent of the smaller browser geometry budget. */
export const FILE_LIMIT = 64 * 1024 * 1024;
export const TOTAL_LIMIT = 192 * 1024 * 1024;
export const GEOMETRY_CACHE_LIMIT = 512 * 1024 * 1024;
