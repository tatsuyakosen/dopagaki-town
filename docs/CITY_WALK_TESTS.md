# エリア選択・実都市街歩きの検証結果

## 2026-09-10：取得・再訪・先読み・対応確認の改善

取得を最大2件ずつにし、中央だけでなく周辺街区も完成データから直接開くよう変更しました。
進行方向による先読み、通信失敗の上限付き自動再試行、事前構築CLI、データ対応確認、近隣の準備済みエリア案内、60秒の性能記録を追加しました。
以下はこの変更での観測値です。後続の記録は過去の実装時点を示します。

| 検証 | 結果と範囲 |
| --- | --- |
| 梅田1kmの構築確認 | 原点34.705, 135.4967の25街区を確認。15街区が構築成功、東側10街区はSOURCE_TOO_LARGE |
| 成功範囲 | x=-2,-1,0の3列、z=-2〜2。計625m×1,000mの区画を生成。全域の通行可能性を確認した意味ではない |
| 街区の容量 | 2,678〜20,654頂点、gzip 34,607〜162,525 bytes |
| 形状の連続性 | 15街区の隣接22境界の地形頂点と共通地物173組が一致。全方向の歩行経路・段差は未検証 |
| 既存形状の維持 | 中央・西隣の生成JSONの内容ハッシュは変更前と同一。形状と出典を維持 |
| 境界を越える移動 | 中央・西隣をNullEngineで再確認。20FPS想定・60FPS想定とも5秒で約35.0015m、落下なし。実機FPSではない |
| 再起動後の隣接街区索引 | 西隣5.22ms、中央3.51ms。各1回、ディスクキャッシュ利用 |
| ローカルHTTPの再訪 | 西隣：ready約27.14ms、gzip受信まで計35.07ms。中央：ready約5.94ms、受信まで計13.26ms。ブラウザ描画なし |
| 初回・未準備街区 | 今回の北隣：カタログ約12.01秒＋取得・構築約29.41秒。うち元データ一部は取得済み。完全なコールド比較ではない |
| 位相別の時間 | downloadSeconds / convertSeconds / terrainSeconds / validateSecondsを記録。ネットワークと変換の遅さを切り分けられる |
| 対応API | 隣接街区のready、gzip配信、近隣候補が200。範囲外街区は400 |
| 丸の内 | 千代田区・中央区のカタログを確認。約200MB規模の元データを含み、容量上限で拒否。構築成功ではない |
| 横浜 | カタログの容量欠落を発見してHEAD確認を実装。ただし配布元から容量を取得できずSOURCE_SIZE_UNKNOWN。構築成功ではない |
| 複数自治体 | 最大4自治体の年度・metadata・ファイル帰属を保持する合成を実装。異なる年度と未知の利用条件を合成テストで検証。実地域の合成完了は未確認 |
| データ取得 | 最大2件の並列上限と、失敗時に処理中の組を待つことをテスト。容量不明時に無制限のGETへ移らないことも検証 |
| 先読み | 進行方向の優先、完成済みなら検索・構築を省くこと、最大5街区の保持・解放、現在地保護、通信再試行をテスト |
| 性能記録 | 可視・歩行中の60秒を対象に平均FPS、95パーセンタイルのフレーム時間、停止回数、最大保持数、取得できる場合のJSヒープを記録。合成フレーム列で検証 |

ローカルの22ファイル・166テスト、lint、型検査、ビルド、クリーンインストールが成功。地図初回JSはgzip 48,430 bytesです。
公開前ガードは作業ツリー113ファイル・取得済み履歴328 blobで指摘0件でした。最終CIの状態はPRを参照してください。

取得・構築の試行では依存関係の再インストール中に2街区がワーカー起動に失敗したため、インストール完了後にその2街区を再試行しました。
上表の25街区の内訳は再試行後です。元データ・生成形状・詳細ログはGitに含めません。

この開発環境では3D目視、実機FPS、GPUメモリ、ブラウザでの長時間歩行・新しい計測UIの操作確認は未実施です。
平均FPSだけでなく長いフレームを記録する機能を追加したもので、実測FPSが改善したという主張ではありません。
原本の事前分割・共有配信、歩道／階段／水域／高架の通行判定、写真外観、AI計画の拡張、実都市PvP接続も今回の変更には含めていません。

### 依存関係

npm audit fix --ignore-scriptsによる互換範囲の更新で、35件（critical 1 / high 9 / moderate 23 / low 2）から14件（high 2 / moderate 12）へ減少しました。
ADKは2.0.0を維持し、実パッケージのインポートとInMemoryRunnerのセッション作成を確認。実LLMへの通信はしていません。
残る監査項目にはADK経由のadm-zip、OpenTelemetry、uuidを含みます。監査のhigh以上を失敗させるCI設定は維持しています。
[adm-zipの監査情報](https://github.com/advisories/GHSA-vwc7-r8mq-g2x9)も確認し、監査を回避する除外や実API未検証を成功扱いする変更はしていません。

## 2026-09-10：地形・隣接街区・操作・再訪

Node.js 24.19.0。以下は今回の追加実装の結果です。後半の2026-09-07記録は移行時の履歴として残しています。

| 項目 | 結果・条件 |
| --- | --- |
| 中央街区 | 96メッシュ、17,666頂点、JSON 605,628 bytes、gzip 146,156 bytes |
| 西隣の街区 | 69メッシュ、10,412頂点、JSON 377,367 bytes、gzip 97,575 bytes |
| 追加地形 | 国土地理院DEMタイル2枚、662,279 bytesを利用。出典とハッシュを生成データへ記録。整備・測量年度は不明 |
| 高さ補正 | 中央は道路31地物・建物2地物、西隣は道路43地物・建物3地物。標高ゼロの対象だけをDEMへ合わせた |
| 地形の高さ | 2街区で局所Y −0.6609〜0.5072m。地面の概形であり、縁石や階段の精度を示す数値ではない |
| 継ぎ目 | 共通境界の21頂点が完全一致。共通地物15個の形状・IDも一致 |
| 境界越え | NullEngineで2街区を同時に読み込み、継ぎ目を越える5秒の走行を確認。落下なし。20FPS想定・60FPS想定とも約35.0015m |
| 中央の衝突 | 接地300フレーム・方向移動480フレーム成功。GPUを使わない検証 |
| キャッシュなしの初回 | カタログ約33.13秒、取得・構築約58.41秒。逐次実行で合計約91.5秒。端末への転送・GPU描画を含まない |
| 元ファイル取得済み | 中央2.787秒／西隣2.562秒。1回ずつの観測でベンチマーク中央値ではない |
| 再起動後の再訪 | 完成した中央街区の索引検索約1.96ms。HTTP経由では約27.60msで取得。再変換・カタログ確認を省略 |
| HTTP配信 | ready APIとステージのgzip配信が200。範囲外の街区番号は400で拒否 |
| 地図初回JS | gzip 47,902 bytes、80KiB上限内。地図画像・3Dエンジン・都市データは含まない |
| 東隣の街区 | カタログ確認で SOURCE_TOO_LARGE。容量上限を上げて回避せず、未取得の境界として扱う |
| ブラウザ | 地図表示、地点操作、WebGL非対応の事前案内を確認。非対応時に構築を開始させない |

20ファイル・152テスト、lint、型検査、ビルド、地図容量ガードが成功しました。
テストは地形パース・欠測拒否・出典・境界一致・道路細分化、取得先制限、低FPS移動、先読みの成功／失敗／中止、地図範囲外とカタログ不一致の拒否を含みます。
追加検査では中央街区内の道路三角形1,931個の重心が地面より1cm超沈んでいる例は0件でした。全地点・全表面の視覚検証ではありません。
公開前ガードは作業ツリー109ファイルと取得済み履歴295 blobで指摘0件でした。GitHub Actionsの最終状態はPRを参照してください。

再現:

```sh
npm test
npm run lint
npm run typecheck
npm run build
npm run check:city-bundle
npm run verify:city-walk -- .local/city-build/stages/<中央街区ID>.json
npm run verify:city-area -- .local/city-build/stages/<西隣ID>.json .local/city-build/stages/<中央街区ID>.json
```

現時点の制約:

- ブラウザがWebGL非対応のため、3D目視・ブラウザ操作での全工程・実機FPS・GPU・メモリは未検証。NullEngineのフレーム想定は実機FPS測定ではない。
- 1km全区画、他自治体、全方向の経路接続、街区追加・解放を伴う長時間ブラウザ歩行は未検証。
- 初回の配布元通信は遅い。完成データ再訪の高速化を初回速度として扱わない。
- DEM補間と標高ゼロ地物の高さ補正は概形。写真外観、歩道・段差・階段・水域・高架の完全再現、実AI、実都市PvPは未達成。
- 原データ・DEMタイル・生成形状・キャッシュ・ローカル検証結果はGitに同梱しない。公開用の認証・利用者別制限・デプロイも未実施。
- 既存依存の監査エラーはこの変更では修正していない。最終状態はCIのaudit gateを参照。

## 2026-09-07：TypeScript移行時の記録

実施日: 2026-09-07。現行版はNode.js 24.19.0 / npm 11.9.0で検証した。
取得・変換・テスト・CIをTypeScriptへ統一し、Pythonの実行ファイル・依存定義・テストを削除した。
起動にはNode.js 22.19以上（22系）または24.13以上とnpmを使用する。

## 実データと移行の比較

公式 [PLATEAU CityGML API](https://docs.plateauview.mlit.go.jp/datasets/citygml/) が解決した大阪市2025年度データを利用。
一般都市モデルのmetadata XMLでCC BY 4.0を確認し、追加制約フィールドがないことを確認した。
TypeScript版でも公式APIの生応答と実metadata ZIPを検証した。測量年度は不明。
2025年度の形状を現在の街の完全再現とは扱わない。

| 対象 | 結果 |
| --- | --- |
| 梅田中心 34.705, 135.4967 | 建物・道路95メッシュ、15,501頂点、LOD上限2 |
| 梅田西側 34.707, 135.491 | 建物・道路134メッシュ、3,778頂点、LOD上限2 |
| 元データ | 建物27,580,155 bytes＋道路1,422,915 bytes＝29,003,070 bytes |
| 中心街区のJSON | 497,447 bytes、gzip 104,728 bytes。出典・制限事項を含む |
| 頂点座標の移行比較 | 両街区の全頂点座標・地物種別・メッシュ数が一致。最大座標差0m |
| 出典の移行比較 | 元ファイル・metadataのURL、SHA-256、年度、利用条件が一致 |
| 三角形の分割 | Earcutの実装・版が異なるためindicesは同一ではない。穴の保持・投影面積を各面で検査 |
| 表面積の差 | 地物ごとの3D表面積の相対差は中心最大約0.011%、西側最大約0.00014%。5cm以内の非平面を許容する既存仕様により分割方法で微差が出る。頂点は動かさない |
| 開始地点 | 中心は同じ。西側は道路の三角形分割に伴い別の安全な候補を選択。両方を衝突検証 |
| 地図初回JavaScript | 47,439 bytes gzip、2静的チャンク。80KiBのCI上限内 |
| 実機FPS／GPU／ブラウザメモリ | 未測定。変換時間・転送量・CPU衝突時間からFPSを推定しない |

2街区は250mの歩行範囲が重ならない別地点だが、同じ大阪市データセットである。
全国対応や他自治体での成功を示す検証ではない。
旧版で東京・丸の内は複数自治体のカタログ結果、福島駅付近は元データ容量上限により拒否した。
現行版にも同じ制限を引き継ぎ、単体テストで拒否を確認している。

元ファイルのSHA-256:

- 建物: `1d4238ea5923cf0cc34dde79cfe4d64ec8d96e3d2593bec2fab28a5fb34f7175`
- 道路: `72e2257971a339f4f978c6305b2c5b4d9f073a9afe4ef52477ae3d745f1b4971`

正確な配信URL、metadata ZIPのSHA-256、年度・条件・取得日は生成CityManifestに含める。
原データと生成形状はGitに同梱しない。

## 変換の速度

同じ実データ・LOD2・元ファイルのキャッシュ2件を使い、旧版と現行版を交互に各3回実行した。
各回で新規子プロセスを起動し、取得ファイルの読込・変換・開始地点検証・JSON出力と親での受信解析までを計測した。
他のビルド・テストを同時に走らせない条件での値。ネットワーク取得、gzip生成、HTTP、GPU描画を含まない。
旧版の比較元は `79bdeb0`、Python 3.12.13 / mapbox-earcut 2.0.0 / NumPy 2.2.6。
現行版はTypeScript / Earcut 3.2.3 / saxes 6.0.0。

| 街区 | 旧Python版・中央値（範囲） | TypeScript版・中央値（範囲） |
| --- | --- | --- |
| 梅田中心 | 1.696秒（1.631〜1.724秒） | 2.392秒（2.385〜2.569秒） |
| 梅田西側 | 1.514秒（1.446〜1.533秒） | 2.447秒（2.430〜2.565秒） |

この環境では再構築が約0.7〜0.9秒増えた。移行を高速化の成果とは扱わない。
Pythonの準備を不要にし、TypeScriptだけで取得からゲームまで開発・起動できることが目的。
完成ジョブの再利用では変換を省略し、地図とゲームのブラウザ用コードへ変換ライブラリを追加していない。
初回のネットワーク取得は配布元に依存する。旧版で確認31.78秒、取得＋変換28.34秒を観測したが、現行版の初回時間を表す値ではない。

## 自動検証

- TypeScript: 18ファイル・137テスト成功。うち変換・取得の36テストを新設し、旧Pythonの14テストを移行・拡充した。
- 変換: 原点・軸、凹形状、穴、垂直面、非平面拒否、入力ハッシュ、重複除外、CRS・XLink・座標・閉鎖リング・出典の検証。
- 取得: 取得先・リダイレクト制限、Content-Lengthなしの容量超過、失敗レスポンスをキャッシュしないこと、ライセンス未確認／追加条件拒否、ZIPの重複・過大・不正パス、XMLのDTD・entity・NUL・非UTF-8拒否。
- 子プロセス: 本物のNodeワーカーによる検証エラーと即時中止を確認。既存ジョブの再利用・取消・不正出力拒否テストも成功。
- 実データのNullEngine: 両街区で上から最初に当たる面が道路であること、接地300フレームで沈まないこと、4方向合計480フレームの移動で座標が有限かつ瞬間移動しないことを確認。
- CPU衝突の合計時間: 中心は接地11ms＋方向移動16ms、西側は16ms＋15ms。GPU描画を行わない値でありFPSではない。
- `npm ci --ignore-scripts`、lint、typecheck、production build、地図容量ガードに成功。最終CI結果はPRのチェックを参照。
- lockfileは変換用依存7件の追加とNode.js最低版の更新に限定し、既存依存のバージョンを変更していない。

再現手順:

```sh
npm ci --ignore-scripts
npm run lint
npm run typecheck
npm test
npm run build
npm run check:city-bundle
npm run verify:city-walk -- .local/city-build/stages/<生成されたID>.json
npm run check:publish
npm run check:publish -- --history
```

手動変換は `npm run convert:city -- ...`。引数は [CITY_WALK.md](CITY_WALK.md) を参照。
変換テストのみなら `npm run test:city-converter`。Python・pip・venvは不要。

## プレビュー構築APIの確認

PATHを空にしてPythonや外部コマンドを検索できない環境で、Node.jsからVite previewを起動した。
HTTP経由の公式カタログ確認 → 構築ジョブ → 完成した95メッシュの受信 → 同じ完成ジョブの再利用が成功した。
元データ2件はキャッシュを再利用し、構築サービスの計測は2.609秒、HTTPとポーリング込みで2.707秒。
完成ジョブ再利用はHTTP込み4.75ms、配信ヘッダーはgzip、圧縮後104,728 bytesだった。
実際のNode子プロセスを使ったAPI検証であり、ブラウザ描画の計測ではない。

## ブラウザと実機の制約

移行前に地図タイル、範囲枠、座標、候補地点、品質操作、確認成功・処理中・失敗の表示を確認した。
当時のブラウザ検証環境にはPythonライブラリの制約があった。現行版はその依存を削除したが、ブラウザ操作の通し検証を再実施したとは扱わない。

同ブラウザはWebGLを利用できず、実3Dの目視・キー移動・滑らかさ・追従カメラの視覚品質・実機FPSは未検証。
単体テストやNullEngineの成功を、見た目やFPSの受入完了とは扱わない。
Windows実機、既存Playwright E2E、長時間soak、モバイル、実AI、Docker／Cloud Runは未検証。

## 依存関係監査と公開前確認

`npm audit` は35件（critical 1 / high 9 / moderate 23 / low 2）を報告。既存ADK等の推移依存を含み、未解消。
今回追加した変換用依存に監査の指摘はなかった。CIのhigh以上のaudit gateを維持し、失敗を隠さない。
公開構築サーバーの認証・利用者別制限・実機検証と依存の修正は引き続き必要。

公開前ガードで秘密情報パターン、大容量ファイル、生都市データの混入を確認する。
指摘0件は情報漏えいが存在しない証明ではない。秘密値・個人の会話・連絡先・生XML・キャッシュを公開変更へ含めない。
GitHubのsecret scanning／push protection設定は変更していない。
